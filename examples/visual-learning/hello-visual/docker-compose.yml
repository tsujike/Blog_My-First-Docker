version: '3.8'

services:
  web:
    build: .
    container_name: hello-visual-web
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped

