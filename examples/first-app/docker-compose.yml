version: '3.8'

services:
  web:
    build: .
    container_name: first-app-web
    ports:
      - "8082:80"  # ポート8080が使用されている場合は8082に変更
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped

